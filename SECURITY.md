# 보안 정책

돈동생은 유저의 **Gmail 읽기**와 **Drive 읽기·쓰기** 권한으로 돌아갑니다.
권한이 넓은 만큼 문제가 생기면 영향도 큽니다.

## 취약점을 발견하셨다면

**공개 이슈로 올리지 말고** 아래로 알려주세요.

- GitHub Security Advisory: [비공개 신고](https://github.com/sbigstar0310/k-money/security/advisories/new)

확인되는 대로 답을 드리고, 고친 뒤에 무엇이 문제였는지 공개하겠습니다.
1인 프로젝트라 대응이 느릴 수 있습니다. 심각한 문제라면 그 사실도 함께 알려 주세요.

## 알려진 위험

숨기지 않고 적습니다.

**Gmail 스코프가 넓습니다.** `gmail.readonly` 는 메일함 전체를 읽을 수 있습니다.
구글이 발신자·라벨 단위의 좁은 권한을 제공하지 않아 줄일 수 없습니다.
코드는 `has:attachment filename:zip (뱅크샐러드 OR banksalad)` 에 걸리는 메일만 봅니다.

**Drive 스코프도 넓습니다.** xlsx 를 Google Sheets 로 변환하려면 Drive API 가 필요하고,
그 API 는 파일 단위 스코프로는 동작하지 않습니다.

**Sheets 스코프도 계정 전체입니다.** `spreadsheets` 는 이 시트만이 아니라 **계정의
모든 스프레드시트**를 읽고 씁니다. 변환한 임시 시트를 `openById` 로 열어야 해서
`spreadsheets.currentonly` 로는 안 됩니다. 아래 `=IMPORTDATA` 경로가 실제 위험이
되는 것도 이 스코프 때문입니다.

**집계 코어는 Apps Script 라이브러리로 배포됩니다.** 유저는 버전을 고정해서 쓰므로
게시자가 새 버전을 올려도 자동으로 적용되지 않습니다. 다만 유저가 버전을 올리는
순간 그 코드를 신뢰하게 됩니다. 그래서 **원격 코드를 받아 실행하지 않습니다** —
네트워크로 받아오는 것이 아예 없습니다. 갱신은 유저가 라이브러리 버전을 직접
올릴 때만 일어납니다.

**라이브러리 코드는 유저의 편집기에 보이지 않습니다.** 배포되는 코드의 대부분
(집계 코어·압축 해제)이 별도 프로젝트에 있어서, 유저는 자기 시트의 `main` 만
읽을 수 있습니다. 같은 코드가 이 저장소의 `appsscript/` 에 있지만, **그것이
지금 도는 코드와 같다는 보장은 게시자를 믿는 것 말고 없습니다.** 라이브러리
방식의 근본 한계입니다.

**비밀번호는 스크립트 속성에 평문으로 저장됩니다.** Apps Script 가 암호화 저장소를
제공하지 않습니다. 시트를 **편집 권한으로 공유하면** 상대가 접근할 수 있습니다.

**비밀번호 힌트는 첫 글자·끝 글자·자릿수를 노출합니다.** 6자 미만이면 자릿수만
보여줍니다 — 짧은 비밀번호는 첫 글자와 끝 글자만으로 후보가 너무 좁아집니다.
편의를 위한 기능이고, 원치 않으면 쓰지 않아도 됩니다.

## 직접 확인하는 법

시트에서 `확장 프로그램 → Apps Script` 를 열면 **이 시트에 붙은 코드** 전문이
보입니다. 집계 라이브러리는 위에 적은 대로 여기 안 뜹니다 — 메일 검색어도
거기 있어서 이 편집기에서 검색하면 안 나옵니다.

- `UrlFetchApp` — 스스로 인터넷 요청을 보내는 통로. **한 곳도 없고**, 매니페스트의
  `oauthScopes` 에 `script.external_request` 도 없습니다. **그 목록을 빠짐없이 적어
  둔 것 자체가 장치**입니다 — 라이브러리는 호출한 스크립트의 권한으로 돌기 때문에,
  목록에 없는 권한은 라이브러리 코드가 쓰려 해도 던집니다.
  이 저장소에서 **유일하게 검증 가능한 진짜 보증**이 이것입니다
- `PropertiesService` — 비밀번호가 어디에 저장되는지
- ⚙️ `프로젝트 설정` → `appsscript.json 매니페스트 파일 표시` — 권한 목록 전문

> ⚠️ **"인터넷 권한이 없으니 데이터가 못 나간다" 는 말은 과장입니다.** 스스로
> HTTP 요청을 못 보낼 뿐입니다. `drive` 권한으로 파일 공유 설정을 바꿔 밖에
> 열어 둘 수 있고, `spreadsheets` 권한으로 시트에 `=IMPORTDATA(...)` 같은 수식을
> 써 넣으면 **구글 서버가 대신 요청을 보냅니다.** 넓은 권한을 준 이상 최종적으로
> 믿는 대상은 권한 목록이 아니라 **코드와 게시자**입니다. 그래서 코드를 공개합니다.

같은 코드가 이 저장소에도 있습니다.
