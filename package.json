{
  "name": "k-money",
  "version": "0.1.1",
  "description": "돈동생 — 집계 코어. node 와 Apps Script 양쪽에서 같은 코드가 돈다.",
  "private": true,
  "scripts": {
    "test": "node --test test/",
    "build": "node scripts/build-gs.js",
    "check": "node scripts/build-gs.js && node --test test/",
    "run:local": "node scripts/run-local.js",
    "version": "node -e \"require('fs').writeFileSync('VERSION', require('./package.json').version + '\\n')\" && node scripts/build-gs.js && git add VERSION appsscript/core.gs"
  },
  "engines": {
    "node": ">=20"
  }
}
